<h1><%= @cocktail.name %></h1>

<ul>
  <% @cocktail.doses.each do |dose| %>
  <li> <%= dose.ingredient.name %>
  <%= dose.description %>
  <%= link_to "delete?", cocktail_dose_path(dose), method: :delete, data: { confirm: "sure?"} %>
  </li>

  <% end %>
</ul>

<p><%= link_to 'add dose?', new_cocktail_dose_path(@cocktail)%></p>

 <p><%= link_to "See all the cocktails", cocktails_path %></p>

 <%= link_to "delete?",cocktail_path(@cocktail), method: :delete, data: { confirm: "sure?"} %>
